{# comes in handy sometimes. #}
{% set isHomepage = (entry is defined ? entry.section.handle == 'homepage' : false) %}

{% set fonts = ['times', 'arial'] %}

{#
any URLs that you want to be preloaded
eg
[
    {
        path: "example.woff2",
        as: "font",
        type: "font/woff2"
    },
]
#}
{% set preloadUrls = [] %}

{# !!! DO NOT REMOVE !!! #}
{% block html %}{% endblock %}